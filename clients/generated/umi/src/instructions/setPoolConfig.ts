/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  SetPoolConfigParams,
  SetPoolConfigParamsArgs,
  getSetPoolConfigParamsSerializer,
} from '../types';

// Accounts.
export type SetPoolConfigInstructionAccounts = {
  admin: Signer;
  perpetuals: PublicKey | Pda;
  pool: PublicKey | Pda;
};

// Data.
export type SetPoolConfigInstructionData = {
  discriminator: Uint8Array;
  params: SetPoolConfigParams;
};

export type SetPoolConfigInstructionDataArgs = {
  params: SetPoolConfigParamsArgs;
};

export function getSetPoolConfigInstructionDataSerializer(): Serializer<
  SetPoolConfigInstructionDataArgs,
  SetPoolConfigInstructionData
> {
  return mapSerializer<
    SetPoolConfigInstructionDataArgs,
    any,
    SetPoolConfigInstructionData
  >(
    struct<SetPoolConfigInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['params', getSetPoolConfigParamsSerializer()],
      ],
      { description: 'SetPoolConfigInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([216, 87, 65, 125, 113, 110, 185, 120]),
    })
  ) as Serializer<
    SetPoolConfigInstructionDataArgs,
    SetPoolConfigInstructionData
  >;
}

// Args.
export type SetPoolConfigInstructionArgs = SetPoolConfigInstructionDataArgs;

// Instruction.
export function setPoolConfig(
  context: Pick<Context, 'programs'>,
  input: SetPoolConfigInstructionAccounts & SetPoolConfigInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey('perpetuals', '');

  // Accounts.
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false as boolean,
      value: input.admin ?? null,
    },
    perpetuals: {
      index: 1,
      isWritable: false as boolean,
      value: input.perpetuals ?? null,
    },
    pool: { index: 2, isWritable: true as boolean, value: input.pool ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: SetPoolConfigInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getSetPoolConfigInstructionDataSerializer().serialize(
    resolvedArgs as SetPoolConfigInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
