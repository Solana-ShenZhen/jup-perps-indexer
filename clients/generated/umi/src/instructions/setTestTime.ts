/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  SetTestTimeParams,
  SetTestTimeParamsArgs,
  getSetTestTimeParamsSerializer,
} from '../types';

// Accounts.
export type SetTestTimeInstructionAccounts = {
  admin: Signer;
  perpetuals: PublicKey | Pda;
};

// Data.
export type SetTestTimeInstructionData = {
  discriminator: Uint8Array;
  params: SetTestTimeParams;
};

export type SetTestTimeInstructionDataArgs = { params: SetTestTimeParamsArgs };

export function getSetTestTimeInstructionDataSerializer(): Serializer<
  SetTestTimeInstructionDataArgs,
  SetTestTimeInstructionData
> {
  return mapSerializer<
    SetTestTimeInstructionDataArgs,
    any,
    SetTestTimeInstructionData
  >(
    struct<SetTestTimeInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['params', getSetTestTimeParamsSerializer()],
      ],
      { description: 'SetTestTimeInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([242, 231, 177, 251, 126, 145, 159, 104]),
    })
  ) as Serializer<SetTestTimeInstructionDataArgs, SetTestTimeInstructionData>;
}

// Args.
export type SetTestTimeInstructionArgs = SetTestTimeInstructionDataArgs;

// Instruction.
export function setTestTime(
  context: Pick<Context, 'programs'>,
  input: SetTestTimeInstructionAccounts & SetTestTimeInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey('perpetuals', '');

  // Accounts.
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false as boolean,
      value: input.admin ?? null,
    },
    perpetuals: {
      index: 1,
      isWritable: true as boolean,
      value: input.perpetuals ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: SetTestTimeInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getSetTestTimeInstructionDataSerializer().serialize(
    resolvedArgs as SetTestTimeInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
