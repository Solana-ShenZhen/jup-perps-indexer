/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  SetCustodyGlobalLimitParams,
  SetCustodyGlobalLimitParamsArgs,
  getSetCustodyGlobalLimitParamsSerializer,
} from '../types';

// Accounts.
export type SetCustodyGlobalLimitInstructionAccounts = {
  keeper: Signer;
  custody: PublicKey | Pda;
};

// Data.
export type SetCustodyGlobalLimitInstructionData = {
  discriminator: Uint8Array;
  params: SetCustodyGlobalLimitParams;
};

export type SetCustodyGlobalLimitInstructionDataArgs = {
  params: SetCustodyGlobalLimitParamsArgs;
};

export function getSetCustodyGlobalLimitInstructionDataSerializer(): Serializer<
  SetCustodyGlobalLimitInstructionDataArgs,
  SetCustodyGlobalLimitInstructionData
> {
  return mapSerializer<
    SetCustodyGlobalLimitInstructionDataArgs,
    any,
    SetCustodyGlobalLimitInstructionData
  >(
    struct<SetCustodyGlobalLimitInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['params', getSetCustodyGlobalLimitParamsSerializer()],
      ],
      { description: 'SetCustodyGlobalLimitInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([94, 82, 154, 177, 193, 205, 141, 76]),
    })
  ) as Serializer<
    SetCustodyGlobalLimitInstructionDataArgs,
    SetCustodyGlobalLimitInstructionData
  >;
}

// Args.
export type SetCustodyGlobalLimitInstructionArgs =
  SetCustodyGlobalLimitInstructionDataArgs;

// Instruction.
export function setCustodyGlobalLimit(
  context: Pick<Context, 'programs'>,
  input: SetCustodyGlobalLimitInstructionAccounts &
    SetCustodyGlobalLimitInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey('perpetuals', '');

  // Accounts.
  const resolvedAccounts = {
    keeper: {
      index: 0,
      isWritable: false as boolean,
      value: input.keeper ?? null,
    },
    custody: {
      index: 1,
      isWritable: true as boolean,
      value: input.custody ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: SetCustodyGlobalLimitInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getSetCustodyGlobalLimitInstructionDataSerializer().serialize(
    resolvedArgs as SetCustodyGlobalLimitInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
