/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  CreateTokenMetadataParams,
  CreateTokenMetadataParamsArgs,
  getCreateTokenMetadataParamsSerializer,
} from '../types';

// Accounts.
export type CreateTokenMetadataInstructionAccounts = {
  admin: Signer;
  perpetuals: PublicKey | Pda;
  pool: PublicKey | Pda;
  transferAuthority: PublicKey | Pda;
  metadata: PublicKey | Pda;
  lpTokenMint: PublicKey | Pda;
  tokenMetadataProgram: PublicKey | Pda;
  systemProgram: PublicKey | Pda;
  rent: PublicKey | Pda;
};

// Data.
export type CreateTokenMetadataInstructionData = {
  discriminator: Uint8Array;
  params: CreateTokenMetadataParams;
};

export type CreateTokenMetadataInstructionDataArgs = {
  params: CreateTokenMetadataParamsArgs;
};

export function getCreateTokenMetadataInstructionDataSerializer(): Serializer<
  CreateTokenMetadataInstructionDataArgs,
  CreateTokenMetadataInstructionData
> {
  return mapSerializer<
    CreateTokenMetadataInstructionDataArgs,
    any,
    CreateTokenMetadataInstructionData
  >(
    struct<CreateTokenMetadataInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['params', getCreateTokenMetadataParamsSerializer()],
      ],
      { description: 'CreateTokenMetadataInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([221, 80, 176, 37, 153, 188, 160, 68]),
    })
  ) as Serializer<
    CreateTokenMetadataInstructionDataArgs,
    CreateTokenMetadataInstructionData
  >;
}

// Args.
export type CreateTokenMetadataInstructionArgs =
  CreateTokenMetadataInstructionDataArgs;

// Instruction.
export function createTokenMetadata(
  context: Pick<Context, 'programs'>,
  input: CreateTokenMetadataInstructionAccounts &
    CreateTokenMetadataInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey('perpetuals', '');

  // Accounts.
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: true as boolean,
      value: input.admin ?? null,
    },
    perpetuals: {
      index: 1,
      isWritable: false as boolean,
      value: input.perpetuals ?? null,
    },
    pool: { index: 2, isWritable: false as boolean, value: input.pool ?? null },
    transferAuthority: {
      index: 3,
      isWritable: false as boolean,
      value: input.transferAuthority ?? null,
    },
    metadata: {
      index: 4,
      isWritable: true as boolean,
      value: input.metadata ?? null,
    },
    lpTokenMint: {
      index: 5,
      isWritable: false as boolean,
      value: input.lpTokenMint ?? null,
    },
    tokenMetadataProgram: {
      index: 6,
      isWritable: false as boolean,
      value: input.tokenMetadataProgram ?? null,
    },
    systemProgram: {
      index: 7,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    rent: { index: 8, isWritable: false as boolean, value: input.rent ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: CreateTokenMetadataInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCreateTokenMetadataInstructionDataSerializer().serialize(
    resolvedArgs as CreateTokenMetadataInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
