/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  AddPoolParams,
  AddPoolParamsArgs,
  getAddPoolParamsSerializer,
} from '../types';

// Accounts.
export type AddPoolInstructionAccounts = {
  admin: Signer;
  transferAuthority: PublicKey | Pda;
  perpetuals: PublicKey | Pda;
  pool: PublicKey | Pda;
  lpTokenMint: PublicKey | Pda;
  systemProgram: PublicKey | Pda;
  tokenProgram: PublicKey | Pda;
  rent: PublicKey | Pda;
};

// Data.
export type AddPoolInstructionData = {
  discriminator: Uint8Array;
  params: AddPoolParams;
};

export type AddPoolInstructionDataArgs = { params: AddPoolParamsArgs };

export function getAddPoolInstructionDataSerializer(): Serializer<
  AddPoolInstructionDataArgs,
  AddPoolInstructionData
> {
  return mapSerializer<AddPoolInstructionDataArgs, any, AddPoolInstructionData>(
    struct<AddPoolInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['params', getAddPoolParamsSerializer()],
      ],
      { description: 'AddPoolInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([115, 230, 212, 211, 175, 49, 39, 169]),
    })
  ) as Serializer<AddPoolInstructionDataArgs, AddPoolInstructionData>;
}

// Args.
export type AddPoolInstructionArgs = AddPoolInstructionDataArgs;

// Instruction.
export function addPool(
  context: Pick<Context, 'programs'>,
  input: AddPoolInstructionAccounts & AddPoolInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey('perpetuals', '');

  // Accounts.
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: true as boolean,
      value: input.admin ?? null,
    },
    transferAuthority: {
      index: 1,
      isWritable: false as boolean,
      value: input.transferAuthority ?? null,
    },
    perpetuals: {
      index: 2,
      isWritable: true as boolean,
      value: input.perpetuals ?? null,
    },
    pool: { index: 3, isWritable: true as boolean, value: input.pool ?? null },
    lpTokenMint: {
      index: 4,
      isWritable: true as boolean,
      value: input.lpTokenMint ?? null,
    },
    systemProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    tokenProgram: {
      index: 6,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
    rent: { index: 7, isWritable: false as boolean, value: input.rent ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: AddPoolInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getAddPoolInstructionDataSerializer().serialize(
    resolvedArgs as AddPoolInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
