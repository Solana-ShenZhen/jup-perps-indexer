/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  SetTestOraclePriceParams,
  SetTestOraclePriceParamsArgs,
  getSetTestOraclePriceParamsSerializer,
} from '../types';

// Accounts.
export type SetTestOraclePriceInstructionAccounts = {
  admin: Signer;
  perpetuals: PublicKey | Pda;
  pool: PublicKey | Pda;
  custody: PublicKey | Pda;
  oracleAccount: PublicKey | Pda;
  systemProgram: PublicKey | Pda;
};

// Data.
export type SetTestOraclePriceInstructionData = {
  discriminator: Uint8Array;
  params: SetTestOraclePriceParams;
};

export type SetTestOraclePriceInstructionDataArgs = {
  params: SetTestOraclePriceParamsArgs;
};

export function getSetTestOraclePriceInstructionDataSerializer(): Serializer<
  SetTestOraclePriceInstructionDataArgs,
  SetTestOraclePriceInstructionData
> {
  return mapSerializer<
    SetTestOraclePriceInstructionDataArgs,
    any,
    SetTestOraclePriceInstructionData
  >(
    struct<SetTestOraclePriceInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['params', getSetTestOraclePriceParamsSerializer()],
      ],
      { description: 'SetTestOraclePriceInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([44, 111, 165, 185, 58, 14, 249, 249]),
    })
  ) as Serializer<
    SetTestOraclePriceInstructionDataArgs,
    SetTestOraclePriceInstructionData
  >;
}

// Args.
export type SetTestOraclePriceInstructionArgs =
  SetTestOraclePriceInstructionDataArgs;

// Instruction.
export function setTestOraclePrice(
  context: Pick<Context, 'programs'>,
  input: SetTestOraclePriceInstructionAccounts &
    SetTestOraclePriceInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey('perpetuals', '');

  // Accounts.
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: true as boolean,
      value: input.admin ?? null,
    },
    perpetuals: {
      index: 1,
      isWritable: false as boolean,
      value: input.perpetuals ?? null,
    },
    pool: { index: 2, isWritable: false as boolean, value: input.pool ?? null },
    custody: {
      index: 3,
      isWritable: false as boolean,
      value: input.custody ?? null,
    },
    oracleAccount: {
      index: 4,
      isWritable: true as boolean,
      value: input.oracleAccount ?? null,
    },
    systemProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: SetTestOraclePriceInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getSetTestOraclePriceInstructionDataSerializer().serialize(
    resolvedArgs as SetTestOraclePriceInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
