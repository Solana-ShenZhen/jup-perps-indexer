/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type GetOraclePriceInstructionAccounts = {
  perpetuals: PublicKey | Pda;
  pool: PublicKey | Pda;
  custody: PublicKey | Pda;
  custodyOracleAccount: PublicKey | Pda;
  custodyPriceUpdate?: PublicKey | Pda;
};

// Data.
export type GetOraclePriceInstructionData = { discriminator: Uint8Array };

export type GetOraclePriceInstructionDataArgs = {};

export function getGetOraclePriceInstructionDataSerializer(): Serializer<
  GetOraclePriceInstructionDataArgs,
  GetOraclePriceInstructionData
> {
  return mapSerializer<
    GetOraclePriceInstructionDataArgs,
    any,
    GetOraclePriceInstructionData
  >(
    struct<GetOraclePriceInstructionData>(
      [['discriminator', bytes({ size: 8 })]],
      { description: 'GetOraclePriceInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([200, 20, 0, 106, 56, 210, 230, 140]),
    })
  ) as Serializer<
    GetOraclePriceInstructionDataArgs,
    GetOraclePriceInstructionData
  >;
}

// Instruction.
export function getOraclePrice(
  context: Pick<Context, 'programs'>,
  input: GetOraclePriceInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey('perpetuals', '');

  // Accounts.
  const resolvedAccounts = {
    perpetuals: {
      index: 0,
      isWritable: false as boolean,
      value: input.perpetuals ?? null,
    },
    pool: { index: 1, isWritable: false as boolean, value: input.pool ?? null },
    custody: {
      index: 2,
      isWritable: false as boolean,
      value: input.custody ?? null,
    },
    custodyOracleAccount: {
      index: 3,
      isWritable: false as boolean,
      value: input.custodyOracleAccount ?? null,
    },
    custodyPriceUpdate: {
      index: 4,
      isWritable: false as boolean,
      value: input.custodyPriceUpdate ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getGetOraclePriceInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
