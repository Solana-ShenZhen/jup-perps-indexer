/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  TransferAdminParams,
  TransferAdminParamsArgs,
  getTransferAdminParamsSerializer,
} from '../types';

// Accounts.
export type TransferAdminInstructionAccounts = {
  admin: Signer;
  newAdmin: PublicKey | Pda;
  perpetuals: PublicKey | Pda;
};

// Data.
export type TransferAdminInstructionData = {
  discriminator: Uint8Array;
  params: TransferAdminParams;
};

export type TransferAdminInstructionDataArgs = {
  params: TransferAdminParamsArgs;
};

export function getTransferAdminInstructionDataSerializer(): Serializer<
  TransferAdminInstructionDataArgs,
  TransferAdminInstructionData
> {
  return mapSerializer<
    TransferAdminInstructionDataArgs,
    any,
    TransferAdminInstructionData
  >(
    struct<TransferAdminInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['params', getTransferAdminParamsSerializer()],
      ],
      { description: 'TransferAdminInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([42, 242, 66, 106, 228, 10, 111, 156]),
    })
  ) as Serializer<
    TransferAdminInstructionDataArgs,
    TransferAdminInstructionData
  >;
}

// Args.
export type TransferAdminInstructionArgs = TransferAdminInstructionDataArgs;

// Instruction.
export function transferAdmin(
  context: Pick<Context, 'programs'>,
  input: TransferAdminInstructionAccounts & TransferAdminInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey('perpetuals', '');

  // Accounts.
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false as boolean,
      value: input.admin ?? null,
    },
    newAdmin: {
      index: 1,
      isWritable: false as boolean,
      value: input.newAdmin ?? null,
    },
    perpetuals: {
      index: 2,
      isWritable: true as boolean,
      value: input.perpetuals ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: TransferAdminInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getTransferAdminInstructionDataSerializer().serialize(
    resolvedArgs as TransferAdminInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
