/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  TestInitParams,
  TestInitParamsArgs,
  getTestInitParamsSerializer,
} from '../types';

// Accounts.
export type TestInitInstructionAccounts = {
  upgradeAuthority: Signer;
  admin: PublicKey | Pda;
  transferAuthority: PublicKey | Pda;
  perpetuals: PublicKey | Pda;
  systemProgram: PublicKey | Pda;
  tokenProgram: PublicKey | Pda;
};

// Data.
export type TestInitInstructionData = {
  discriminator: Uint8Array;
  params: TestInitParams;
};

export type TestInitInstructionDataArgs = { params: TestInitParamsArgs };

export function getTestInitInstructionDataSerializer(): Serializer<
  TestInitInstructionDataArgs,
  TestInitInstructionData
> {
  return mapSerializer<
    TestInitInstructionDataArgs,
    any,
    TestInitInstructionData
  >(
    struct<TestInitInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['params', getTestInitParamsSerializer()],
      ],
      { description: 'TestInitInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([48, 51, 92, 122, 81, 19, 112, 41]),
    })
  ) as Serializer<TestInitInstructionDataArgs, TestInitInstructionData>;
}

// Args.
export type TestInitInstructionArgs = TestInitInstructionDataArgs;

// Instruction.
export function testInit(
  context: Pick<Context, 'programs'>,
  input: TestInitInstructionAccounts & TestInitInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey('perpetuals', '');

  // Accounts.
  const resolvedAccounts = {
    upgradeAuthority: {
      index: 0,
      isWritable: true as boolean,
      value: input.upgradeAuthority ?? null,
    },
    admin: {
      index: 1,
      isWritable: false as boolean,
      value: input.admin ?? null,
    },
    transferAuthority: {
      index: 2,
      isWritable: true as boolean,
      value: input.transferAuthority ?? null,
    },
    perpetuals: {
      index: 3,
      isWritable: true as boolean,
      value: input.perpetuals ?? null,
    },
    systemProgram: {
      index: 4,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    tokenProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: TestInitInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getTestInitInstructionDataSerializer().serialize(
    resolvedArgs as TestInitInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
